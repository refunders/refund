#' Functional Principal Component Analysis
#' 
#' 

fpca <- function(X, argvals=seq(0,1,length=ncol(X)), method="sc",
                 random.int = FALSE, nbasis = 10, pve=.00, npc=NULL,
                 var=FALSE, simul=FALSE, sim.alpha=.95, useSymm=FALSE,
                 makePD=FALSE, center=TRUE, cov.est.method=2) {
  
  
  
}