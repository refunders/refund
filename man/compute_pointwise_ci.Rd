% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pffr-methods.R
\name{compute_pointwise_ci}
\alias{compute_pointwise_ci}
\title{Per-point pointwise critical value and reference df}
\usage{
compute_pointwise_ci(
  crit_mode,
  level,
  linear_map,
  df_ctx = NULL,
  crit_df_const = NA_real_
)
}
\arguments{
\item{crit_mode}{One of `"z"`, `"tG1"`, `"satterthwaite"`.}

\item{level}{Confidence level.}

\item{linear_map}{List returned by [build_coef_linear_map()] (only needed for
`"satterthwaite"`).}

\item{df_ctx}{A [pffr_df_context()] result (only needed for
`"satterthwaite"`).}

\item{crit_df_const}{Constant df for `"tG1"` (\eqn{G-1}).}
}
\value{
A list with `crit` (scalar or per-point vector) and `df` (per-point
  vector; `Inf` for `"z"`).
}
\description{
Computes the pointwise interval half-width multiplier for one term under the
chosen reference (S3): `"z"` (Gaussian, reported df `Inf`), `"tG1"`
(\eqn{t_{G-1}}, constant df), or `"satterthwaite"` (per-point Bell-McCaffrey
df from `df_ctx`, with a Gaussian fallback at any zero-variance / undefined
point).
}
\keyword{internal}
