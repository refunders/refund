% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pffr-core.R
\name{pffr_compute_sandwich}
\alias{pffr_compute_sandwich}
\title{Compute a robust covariance matrix from a model-based gam}
\usage{
pffr_compute_sandwich(
  b,
  type,
  cluster_id,
  freq = FALSE,
  dof_correction = "none",
  edf_type = "trace",
  b2 = TRUE,
  center_scores = FALSE,
  cl2_adjustment = "auto"
)
}
\arguments{
\item{b}{Stripped gam object with model-based covariance matrices.}

\item{type}{One of `"cluster"`, `"cl2"`, `"hc"`, `"none"`.}

\item{cluster_id}{Integer cluster vector (for `"cluster"`/`"cl2"`), else
`NULL`.}

\item{freq}{If `TRUE`, frequentist sandwich (`B2 = 0`).}

\item{dof_correction, edf_type}{CR1 small-sample dof options (`"cluster"`
only).}

\item{b2}{Internal ablation switch (default `TRUE`); drop the additive
\eqn{B_2} term when `FALSE` (X5). Applies to `"cluster"`/`"cl2"` only.}

\item{center_scores}{Internal ablation switch (default `FALSE`); center the
per-cluster score sums before the meat when `TRUE` (X6). Applies to
`"cluster"`/`"cl2"` only.}

\item{cl2_adjustment}{CL2 leverage adjustment (`"auto"`, `"exact"`, or
`"shortcut"`), used only for `type = "cl2"`.}
}
\value{
A covariance matrix (with CL2 leverage attributes for `type =
  "cl2"`).
}
\description{
Single dispatch point used by both fit-time correction and on-demand
recomputation. `b` must be a (stripped) gam whose `Vp`/`Vc`/`Ve` are the
model-based (penalized) matrices used as the sandwich bread.
}
\keyword{internal}
